name: Angular Docker Playwright CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:

      # ======================
      # 1. CODE
      # ======================
      - name: Checkout repository
        uses: actions/checkout@v4

      # ======================
      # 2. DOCKER BUILD + UP
      # ======================
      - name: Build and start services
        run: docker compose -f docker-compose.ci.yml up --build -d

      # ======================
      # 3. WAIT FRONTEND READY
      # ======================
      - name: Wait for Angular frontend
        run: |
          echo "Waiting frontend..."
          for i in {1..40}; do
            curl -s http://localhost:4200 > /dev/null && echo "Frontend OK" && exit 0
            sleep 2
          done
          echo "Frontend failed" && docker compose logs && exit 1

      # ======================
      # 4. WAIT API READY
      # ======================
      - name: Wait for API
        run: |
          echo "Waiting API..."
          for i in {1..40}; do
            curl -s http://localhost:3000 > /dev/null && echo "API OK" && exit 0
            sleep 2
          done
          echo "API failed" && docker compose logs && exit 1

      # ======================
      # 5. RUN PLAYWRIGHT TESTS
      # ======================
      - name: Run Playwright tests
        run: docker compose -f docker-compose.ci.yml run tests

      # ======================
      # 6. CLEANUP
      # ======================
      - name: Stop containers
        if: always()
        run: docker compose -f docker-compose.ci.yml down
